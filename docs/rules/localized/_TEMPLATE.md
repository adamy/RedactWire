# <Culture> — `<culture-code>` PII rules

> Copy this file for each new country. Keep columns identical so packs stay comparable.
> One row per rule. `Type` = `PiiType` enum value. `Severity` = Critical/High/Medium/Low
> (see [`../severity.md`](../severity.md); primary overlap key). `Checksum` = validator
> gating the match (a fail DROPS the candidate). `Conf` = base/validated confidence.

Status: **draft | implemented | tested**

## Rules

| Rule | Type | Severity | Pattern (gist) | Checksum | Conf | Notes |
|---|---|---|---|---|---|---|
| | | | | | | |

## Test vectors

Every rule needs positive + negative + boundary.

| Rule | Input | Expect |
|---|---|---|
| | | |

## Notes / gotchas

-
